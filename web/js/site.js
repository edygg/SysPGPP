$(document).ready(function() {
  // Init Materialize components
  $('.button-collapse').sideNav();
  $('.slider').slider({ indicators: false });
  $('.parallax').parallax();
  // End init Materialize components
});